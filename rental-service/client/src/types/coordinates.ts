export type City = {
    title: string,
    // lat: number,
    // lng: number,
    zoom: number,
    location: Point
};

export type Point = {
    id: string,
    title: string,
    lat: number,
    lng: number,
    zoom: number,
}
